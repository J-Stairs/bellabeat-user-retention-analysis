## Case Study: Beyond the Averages — Analyzing User Churn & Device Abandonment in Smart Wearables
**Author:** Jennifer Stairs
**Core Stack:** Google BigQuery (SQL) | Google Sheets | Tableau Public

--

### The Backstory & The "Why"

This project originated as part of the Google Data Analytics Professional curriculum, which provides a standard corporate brief for the smart-wearable company Bellabeat.
However, while the standard guidelines suggest calculating high-level cohort averages to compare against baseline medical benchmarks, I noticed something deeper during the ingestion phase. The standard averages were masking a massive business problem: severe user churn. Most smart device case studies follow a predictable formula: calculate average step counts, compare them to a generic baseline, and call it a day. But when I first opened the public domain FitBit Fitness Tracker dataset, the standard cohort averages felt incomplete.
I didn't want to just look at what "ideal" users do. I wanted to look at the cracks in the ecosystem. What happens when the novelty of a new wellness device wears off? Why do people buy health trackers, only to stick them in a dresser drawer two weeks later?
By shifting the lens from rigid medical benchmarks to user psychology and retention pipeline analysis, I uncovered a massive adoption bottleneck that changes how we look at smart-wearable customer behavior.

--

### Data Source Description & Integrity Critique 

- **Source Legitimacy:** The data is pulled from the public domain **FitBit Fitness Tracker Data** (distributed via Kaggle user Möbius, CC0 license). It tracks minute-level personal biometrics generated by respondents via Amazon Mechanical Turk.
- **Sample Size & Scope:** The dataset spans a 30-day window and consists of distinct logs for 33 eligible tracker users (24 users present in the sleep logs).
- **Vetted Baselines:** To ground individual trends in clinical reality, behavioral patterns were benchmarked against global health metrics established by the **CDC/WHO** (for daily step counts) and the **NHANES (National Health and Nutrition Examination Survey)** historical data (for a 7-hour baseline female sleep average).
- **Data Limitations & Bias Log:**
	1. **Sample Size Constraints:** A cohort of 33 users is far too small to establish statistically significant global population averages without a high margin of error.
	2. **Demographic Blindspots:** The dataset is entirely anonymous and completely lacks demographic metadata such as gender, age, or geographic location. Because Bellabeat specifically engineers health and            wellness products tailored for women, analyzing a generic, gender-blind cohort introduces potential sample bias.
	3.**Data Volatility Outliers:** While auditing individual files, I identified severe data constraints, such as User 1844505072, who logged a massive sleep average of 10.87 hours, but only across 3 logged days. This high volatility reinforces that short-term tracking spans cannot be relied upon to project sustainable user lifestyle trends.

--

### The Technical Friction: Fighting BigQuery Schemas

Before discovering any insights, I had to actually get the data into a warehouse. This is where the project nearly ground to a halt.
When attempting a raw CSV ingestion into Google BigQuery, the pipeline crashed repeatedly with a frustrating error string:

CSV table encountered too many errors, giving up. Rows: 0; errors: 100

The cloud parser was rejecting the file layout due to chaotic trailing floating-point decimals (calculated out past 9 decimal places) and hidden byte-order mark (BOM) encoding artifacts hidden inside the source strings.
**The Staging Fix:** Instead of fighting the cloud parser blindly, I built an intermediate staging layer inside Google Sheets. Passing the raw assets through Sheets stripped the problematic trailing precision data and cleanly standardized the row metadata. Re-exporting this sanitized layout allowed BigQuery's auto-detect schema engines to map the table perfectly on the very next pass.

--

### Advanced Cohort Retention & Compliance Query
To uncover the retention data layer, I wrote a cross-table analysis script. This script handles cross-table typecasting mismatches (INT64 vs STRING user IDs) that would otherwise break a standard JOIN, flags passive device abandonment via logical condition filtering, and segments the cohort by retention risk indicators:

/*
===============================================================================
This query calculates device compliance and tracking habits across a 30-day window.
It aims to pinpoint exactly when and where users abandon their trackers,
differentiating between active use, passive abandonment (leaving the device
on a surface), and a complete drop-off in data logging.


CHANGELOG:
  - Standardized user IDs to STRING to resolve cross-table JOIN type mismatches.
===============================================================================
*/


WITH activity_summary AS (
  SELECT
    CAST(Id AS STRING) AS fitbit_user_id, -- Standardized to STRING
    -- Count how many unique days this user actually logged data
    COUNT(DISTINCT ActivityDate) AS total_days_activity_logged,
   
    -- Track 'Passive Abandonment': Count days where the device was turned on
    -- but recorded exactly 0 steps and nearly 24 hours of sedentary time
    COUNT(CASE
            WHEN CAST(TotalSteps AS INT64) = 0
             AND CAST(SedentaryMinutes AS FLOAT64) >= 1380 -- 1380 mins = 23 hours
            THEN 1
          END) AS passive_abandonment_days,
         
    -- Calculate their average steps strictly on the days they actually wore it
    ROUND(AVG(CASE WHEN CAST(TotalSteps AS INT64) > 0 THEN CAST(TotalSteps AS INT64) END), 0) AS avg_steps_on_active_days
  FROM
    `smooth-aura-494613-b7.bellabeat_wellness.daily_activity_raw`
  GROUP BY
    Id
),


sleep_summary AS (
  SELECT
    CAST(Id AS STRING) AS fitbit_user_id, -- Standardized to STRING
    -- Count how many unique nights this user wore the tracker to bed
    COUNT(DISTINCT SleepDay) AS total_days_sleep_logged
  FROM
    `smooth-aura-494613-b7.bellabeat_wellness.daily_sleep_raw`
  GROUP BY
    Id
)


-- Combine activity tracking and sleep tracking behavior side-by-side
SELECT
  act.fitbit_user_id,
  act.total_days_activity_logged,
 
  -- If a user never logged sleep, replace the null value with 0 to keep data clean
  COALESCE(slp.total_days_sleep_logged, 0) AS total_days_sleep_logged,
  act.passive_abandonment_days,
  act.avg_steps_on_active_days,
 
  -- Logical Flag: Highlight users who dropped off before the full 30 days ended
  CASE
    WHEN act.total_days_activity_logged < 25 THEN 'High Churn Risk (<25 Days Logged)'
    ELSE 'Consistent Logger'
  END AS user_retention_segment


FROM
  activity_summary AS act
LEFT JOIN
  sleep_summary AS slp
  ON act.fitbit_user_id = slp.fitbit_user_id
ORDER BY
  total_days_activity_logged ASC,
  total_days_sleep_logged ASC;

--

### The Core Mystery: Tracking The Ghosts in the Data

While inspecting the data logs generated by my compliance query, I noticed something bizarre. Look closely at the behavior of User 4388161847:

[Insert Line 3 sleep tracking Here: visualizations/Line 3 sleep tracking.png]

- **Total Days Activity Logged:** 8 days
- **Total Days Sleep Logged:** 23 days
- **Passive Abandonment Days:** 7 days
- **Average Daily Steps:** Null / 0

This specific user managed to log **23 distinct nights of sleep metrics**, but only **8 total days of daytime activity**. Even weirder? On 7 of those 8 activity days, their step count was exactly **0**, while their sedentary time clocked in at over **23 hours**.
**The Breakthrough:** The device wasn't broken, and the data wasn't "bad." This is empirical proof of **Passive Abandonment.** This user highly valued sleep tracking, but the moment they woke up, they stripped the tracker off and left it running on a flat surface during the day. This highlights a clear physical comfort barrier or daytime style friction point that a hardware team needs to solve.

--

### Visualizing the Cliff: The Day 12 Drop-off

The metrics were mapped side-by-side using Tableau, applying Bellabeat's signature corporate palette (Blush Rose Gold #E5A995 and Muted Slate Gray #7E8D9C) to contrast baseline compliance. The result wasn't a slow, gradual decline. It was a literal cliff.

#### 1. Performance Tracking Dashboard

[Insert Performance Dashboard Here: visualizations/bellabeat_executive_dashboard.png]

#### 2. The Day 12 Retention Cliff

[Insert Retention Cliff Histogram Here: visualizations/user_retention_cliff.png]

#### Core Analysis Findings:

- **The 12-Day Retention Wall:** Device abandonment isn't a gradual, linear decay over the month. Instead, the cohort hits a collective wall: **48% of all tracking users completely ceased logging data on exactly Day 12**. This isolates the critical window for habit formation to the first 10 days post-purchase.
- **The Sleep-Tracking Friction Point:** 27% of active daily step-trackers completely refused to wear the device to bed (logging 0 total nights of sleep data). This highlights a physical or psychological comfort barrier to 24/7 biometric compliance.
- **Passive Day-Time Abandonment (User Anomaly 4388161847):** This user successfully captured 23 days of sleep data but recorded physical activity on only 8 days. Of those 8 active days, 7 registered exactly 0 steps and 23+ hours of sedentary time. This provides empirical evidence of a user who actively values sleep tracking but finds the device intrusive during working hours, leaving it powered on but stationary on a flat surface.

--

###Transforming Churn into Product Strategy
To patch this leaky customer funnel and transform early tracker usage into long-term lifestyle habits, I developed three concrete feature recommendations tailored for Bellabeat's ecosystem:

1. **The "First-Week Wins" Gamification Engine:** Shift focus away from intimidating, rigid performance targets (like the standard 10,000-step baseline) during the initial onboarding sequence. Implement a "Habit Streak" rewards system based strictly on device compliance ("Wore the tracker 5 days in a row!") to lower psychological friction and solidify early adoption routines.
2. **Predictive Abandonment App Nudges:** Build automated low-engagement detection algorithms directly into the software. If a tracking device registers 0 steps alongside near-24-hour sedentary metrics for a 48-hour window (indicating it has been left abandoned on a dresser), trigger a tailored smartphone push notification: "Hey! Notice we haven't stepped together in a bit. Need a quick charge, or are we ready for a fresh 10-minute stretch?"
3. **Frictionless Sleep Integration Prompts:** To lower the entry barrier for users who resist night-time compliance, introduce a customizable "Wind-Down" mobile notification an hour before their target bedtime, reminding them to pop the tracker back on. Pair this with a hardware "Stealth Mode" that completely darkens the display at night to eliminate light disruption.
